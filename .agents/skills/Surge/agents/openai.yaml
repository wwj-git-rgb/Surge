interface:
  display_name: "Surge"
  short_description: "Repo-specific patterns and workflows for Surge"
  default_prompt: "Use the Surge repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true